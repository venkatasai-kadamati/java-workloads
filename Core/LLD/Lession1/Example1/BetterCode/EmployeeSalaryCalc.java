package Core.LLD.Lession1.Example1.BetterCode;

// This code is a refactored version of the Employee class that adheres to the Single Responsibility Principle (SRP).

// The current code only handles Employee Salary computation.

public class EmployeeSalaryCalc {
    public double computeSalary(Employee e) {
        // code to compute salary
        return 50000.0;  // fixed salary
    }
}
